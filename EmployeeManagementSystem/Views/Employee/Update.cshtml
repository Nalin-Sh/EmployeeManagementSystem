@model EmployeeManagementSystem.Application.DTOs.Employee.EmployeeResponseDTO

@{
    ViewData["Title"] = "Edit Employee";
}

<h2>Edit Employee</h2>

<form asp-action="Edit" method="post">
    <input type="hidden" asp-for="EmployeeID" />

    <div class="mb-3">
        <label asp-for="Name" class="form-label">Employee Name</label>
        <input asp-for="Name" class="form-control" required />
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="DepartmentName" class="form-label">Department</label>
        <select class="form-control" asp-for="DepartmentName">
            @foreach (var dept in ViewBag.Departments)
            {
                <option value="@dept.DepartmentName" selected="@(dept.DepartmentName == Model.DepartmentName)">
                    @dept.DepartmentName
                </option>
            }
        </select>
        <span asp-validation-for="DepartmentName" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Salary" class="form-label">Salary</label>
        <input asp-for="Salary" type="number" step="0.01" class="form-control" required />
        <span asp-validation-for="Salary" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="DateOfJoining" class="form-label">Date of Joining</label>
        <input asp-for="DateOfJoining" type="date" class="form-control" required />
        <span asp-validation-for="DateOfJoining" class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-primary">Save Changes</button>
    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
